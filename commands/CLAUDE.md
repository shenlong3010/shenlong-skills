# commands/ — directory guide

Loaded when working in this directory; root globals apply. Commands are human-invoked slash operations — flat `.md` files, invoked as `/name`.

## Structure
**Invocation** (exact syntax, args, defaults) → **Behavior** (numbered steps) → the artifact or output the user receives.

## Laws
- Behavior steps read **real state** — the staged diff, the actual file, the live branch — never session memory (`commit-writer` runs `git diff --cached`; it does not summarize what it remembers changing).
- Side-effectful steps state exactly what is written where; anything irreversible stops for explicit confirmation instead of executing.
- Creators (`create-*`) route through `tools/scaffold.py` — never duplicate scaffolding logic in a command body.
- Rules of thumb for placement: invoked by the human at a moment in time → command; consumed by the agent mid-task → skill.
- Every command carries a `flow:` key (vocabulary in `tools/validate.py`) — it slots the command into a workflow stage and drives the generated catalog below.
- New commands via `/create-command`.

## Catalog (generated — do not hand-edit)

Grouped by flow. Regenerate with `python3 tools/gen-index.py` after any add/rename.

<!-- INDEX:BEGIN (generated by tools/gen-index.py) -->
**Plan — shape the work before code**
- `brainstorm` — Socratic refinement of a rough idea into a design brief — before any plan exists
- `decompose` — Break a goal, story, or feature into verifiable task units with machine-checkable acceptance and explicit file…

**Execute — build it (loop or TDD)**
- `ralph` — User surface of the ralph loop — /ralph plan|next|auto|status|replan|tickets
- `test-scaffold` — Generate a unit-test skeleton for a target file or function, matching the repo's existing test framework and c…

**Deliver — commits, PRs, docs, artifacts**
- `commit-writer` — Turn the currently staged diff into a conventional-commit message
- `gen-diagram` — Generate a Mermaid diagram (sequence, flowchart, ERD, state, class) from code, a description, or a conversation
- `pr-writer` — Generate a PR title and body from the branch diff against the base branch
- `terse-rewrite` — Rewrite a draft into terse, technically dense engineering prose — no filler, no hedging, no throat-clearing

**Session — context, cost, handoffs**
- `context-audit` — Report what is currently occupying context — files, tool outputs, conversation — with rough token weight and t…
- `handoff-writer` — Capture the current session's state into a HANDOFF.md that a fresh session can act on without the transcript

**Util — library how-tos for scripts**
- `regex-forge` — Convert a natural-language matching requirement into a regex with test cases, or explain an existing regex pie…

**Meta — build the toolbox itself**
- `create-agent` — Scaffold a new agent in this toolbox with standard frontmatter, correct directory placement, and immediate val…
- `create-command` — Scaffold a new command in this toolbox with standard frontmatter, correct directory placement, and immediate v…
- `create-hook` — Scaffold a new hook in this toolbox with standard frontmatter, correct directory placement, and immediate vali…
- `create-skill` — Scaffold a new skill in this toolbox with standard frontmatter, correct directory placement, and immediate val…
- `create-tool` — Scaffold a new tool in this toolbox with standard frontmatter, correct directory placement, and immediate vali…
- `eval-writer` — Extract a golden eval case from a run trace or a completed task and emit a .eval.yml next to the relevant skill

**Career — interview and resume practice**
- `resume-impact` — Rewrite a resume bullet into accomplished-X-by-Y-resulting-in-Z form with a defensibility check — every claim …
<!-- INDEX:END -->
