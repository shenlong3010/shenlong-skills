# Copilot instructions — <repo>

Injected into every request — kept short on purpose. Full ruleset: `archive/RULES-MASTER.md`.

## Non-negotiable constraints
<3–4 lines: the hard constraints>

## Before claiming done
Run and pass: `<verify command>`.

## Behavior rules (distilled)
<the 17 rules compressed to a single flowing paragraph, separated by ·>
