# AGENTS.md

Instructions for any coding agent in this repository. Tool-agnostic mirror of `CLAUDE.md`; full behavioral rules: `archive/RULES-MASTER.md`. Directory-level CLAUDE.md files exist for Claude Code; on other surfaces treat them as reference documents — nothing auto-loads.

## Constraints
<the same hard constraints as CLAUDE.md, verbatim>

## Verify
```bash
<the single done-gate command>
```

## Rules (distilled)
<the 17 rules as one-liners>
