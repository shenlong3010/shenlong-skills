# skills/ — directory guide

Loaded when working in this directory; the root `CLAUDE.md` globals apply on top. This file carries only skill-specific conventions — never restate global rules in skill bodies; they are ambient.

## What a skill is
Agent-consumed knowledge for mid-task use: one folder per skill, `SKILL.md` inside, optional `assets/` and `references/` alongside (see `plan-writer/`).

## Frontmatter
Root spec applies (flat keys), including the `flow:` key — the workflow stage this skill serves (vocabulary in `tools/validate.py`); it drives the grouped catalog below. The `description` must contain explicit trigger language — "Use when / whenever / for …" with concrete phrases a user would actually type — and, where the skill is confusable with a sibling, a "Do NOT use for …" boundary. `skill-lint` enforces this; undertriggering is the common failure mode. Phrase triggers as **events, not topics**: the literal phrases the moment produces ("about to grep", "port already in use", an error string) — models route on literal triggers, not topic labels.

## Body shape
1. **Purpose / when** — one short block.
2. **Method** — the procedure, or per-case procedures for classify-then-extract skills (see `read-image`).
3. **Gotchas** — the skill's real value. Every gotcha must be earned and verifiable; gotchas in library skills are smoke-tested against the real library before shipping (the `data-csv` dtype trap and `config` Norway problem are asserted, not asserted-about).
   - **Both platforms or say so:** any skill whose methods are OS-dependent carries both the Linux and Windows lanes (the `system-lookup` pattern), or explicitly names the platform gap and its fallback (the `file-find` plocate note). A silently Linux-only method on a Windows dev box is a routing lie — the 2026-07 campaign scored it FAIL.
4. **Boundaries** — route to sibling skills *by name* (`image-ocr`, `read-diagram`, `image-prep` pattern). Never fake an unsupported capability: state "not supported" and route (the `pptx` slide-copy rule).

## Conventions
- Names are generic capability names: `docx`, not `docx-py`; no implementation suffixes. Vendored third-party skills keep their upstream identity (`crawl4ai`, `image-ocr`) — renaming muddies provenance.
- One source of truth: if another skill owns a method, point to it — don't restate it.
- New skills only via `/create skill <name>`; migrated or adapted content passes the scan before its first commit and carries `derivation:` + `source:`.

## Catalog (generated — do not hand-edit)

One line per skill, grouped by `flow:`, for discovery while authoring here. Regenerate with `python3 tools/gen-index.py` after any add/rename; the standing-cost estimate it prints is the real front-load bill (descriptions only — bodies load on trigger).

<!-- INDEX:BEGIN (generated by tools/gen-index.py) -->
**Review — pre-ship checks**
- `sql-review` ·data· — Review a SQL query for plan risks, index needs, and correctness traps

**Debug — find and fix root cause**
- `stacktrace-analyzer` ·code· — Analyze a stack trace or exception dump into ranked root-cause hypotheses with concrete next checks
- `systematic-debug` ·process· — Structured root-cause debugging — reproduce, observe, hypothesize, isolate, fix the cause, add the regression …
- `log-triage` ·system· — Triage a raw log dump into clustered errors, a timeline, and the anomaly that matters

**Lookup — find code/data/facts mid-task**
- `repo-index` ·agent· — Pre-build local search indexes — ctags, plocate/updatedb, git commit-graph, a gron cache — so later lookups ar…
- `code-search` ·code· — Find code and context efficiently — the lexical → structural → semantic search ladder with token-budget discip…
- `dependency-lookup` ·code· — Answer "where does this symbol/class/package come from" and "who pulls this dependency in" across Maven/Java, …
- `explain-code` ·code· — Comprehend a specific piece of code — a function, class, module, or file you're staring at — into what it does…
- `symbol-lookup` ·code· — Find symbol definitions, references, and approximate call hierarchies — the ctags/ast-grep lane between grep a…
- `trace-flow` ·code· — Follow one execution or data path across files — a request from entry point to response, a value from source t…
- `data-query` ·data· — Query JSON/YAML/structured data precisely with jq, gron, and yq instead of grepping it
- `diff-read` ·git· — Read large diffs token-efficiently — stat first, then targeted per-file hunks, word-diff for prose, function-c…
- `git-search` ·git· — Search git history efficiently — pickaxe, regex diffs, line-range history, rename-following, true-origin blame
- `env-probe` ·system· — Probe the actual environment before assuming it — tool presence/versions, Python/Node runtime, shell dialect, …
- `file-find` ·system· — Find files fast by name, age, or size — fd for scoped search, plocate for instant system-wide indexed lookup, …
- `system-lookup` ·system· — Find what's happening on a machine — which process holds a port or file, what a process is running with, where…

**Session — context, cost, handoffs**
- `caching` ·agent· — Caching strategy across the AI stack — prompt/prefix caching, MCP and tool-response caching, session file-read…
- `reasoning-budget-guidance` ·agent· — Pick the model tier and reasoning effort per task class instead of defaulting to maximum everywhere

**Util — library how-tos for scripts**
- `bulk-reader` ·agent· — "Delegate bulk file reading to a Haiku subagent
- `code-writer` ·agent· — "Delegate boilerplate code generation to a Haiku subagent
- `config` ·data· — Read and write JSON, YAML, and TOML config safely — safe_load only, the YAML Norway problem, datetime serializ…
- `data-csv` ·data· — Read, clean, and write CSV/tabular data with pandas (and stdlib csv for streaming) — dtype control, encodings,…
- `markup` ·data· — Parse and extract from HTML and XML — BeautifulSoup selectors, parser choice, encodings, and the XML namespace…
- `sqlite` ·data· — Use SQLite from Python (stdlib sqlite3) correctly — parameterized queries, transactions, Row access, WAL, prag…
- `http-requests` ·web· — Call HTTP APIs robustly with requests — sessions, retries with backoff, timeouts, streaming, auth, corporate-p…

**Meta — build the toolbox itself**
- `config-audit` ·agent· — Audit a Claude Code setup — skills, CLAUDE.md, hooks, commands, MCP servers, output-style injectors — for dead…
<!-- INDEX:END -->
