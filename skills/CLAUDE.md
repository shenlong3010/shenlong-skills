# skills/ — directory guide

Loaded when working in this directory; the root `CLAUDE.md` globals apply on top. This file carries only skill-specific conventions — never restate global rules in skill bodies; they are ambient.

## What a skill is
Agent-consumed knowledge for mid-task use: one folder per skill, `SKILL.md` inside, optional `assets/` and `references/` alongside (see `plan-writer/`).

## Frontmatter
Root spec applies (flat keys), including the `flow:` key — the workflow stage this skill serves (vocabulary in `tools/validate.py`); it drives the grouped catalog below. The `description` must contain explicit trigger language — "Use when / whenever / for …" with concrete phrases a user would actually type — and, where the skill is confusable with a sibling, a "Do NOT use for …" boundary. `skill-lint` enforces this; undertriggering is the common failure mode. Phrase triggers as **events, not topics**: the literal phrases the moment produces ("about to grep", "port already in use", an error string) — models route on literal triggers, not topic labels.

## Body shape
1. **Purpose / when** — one short block.
2. **Method** — the procedure, or per-case procedures for classify-then-extract skills (see `read-image`).
3. **Gotchas** — the skill's real value. Every gotcha must be earned and verifiable; gotchas in library skills are smoke-tested against the real library before shipping (the `data-csv` dtype trap and `config` Norway problem are asserted, not asserted-about).
4. **Boundaries** — route to sibling skills *by name* (`image-ocr`, `read-diagram`, `gen-diagram` pattern). Never fake an unsupported capability: state "not supported" and route (the `pptx` slide-copy rule).

## Conventions
- Names are generic capability names: `docx`, not `docx-py`; no implementation suffixes. Vendored third-party skills keep their upstream identity (`crawl4ai`, `image-ocr`) — renaming muddies provenance.
- One source of truth: if another skill owns a method, point to it — don't restate it.
- New skills only via `/create-skill`; migrated or adapted content passes the scan before its first commit and carries `derivation:` + `source:`.

## Catalog (generated — do not hand-edit)

One line per skill, grouped by `flow:`, for discovery while authoring here. Regenerate with `python3 tools/gen-index.py` after any add/rename; the standing-cost estimate it prints is the real front-load bill (descriptions only — bodies load on trigger).

<!-- INDEX:BEGIN (generated by tools/gen-index.py) -->
**Plan — shape the work before code**
- `plan-writer` — Write an execution-ready plan that another agent — Claude Code by default — can run top-to-bottom without prio…

**Execute — build it (loop or TDD)**
- `git-worktrees` — Isolate each work stream in its own git worktree — parallel sessions without branch-switching stomps, clean te…
- `ralph-next` — Orchestrator state machine of the ralph loop — read run state from disk, pick the one eligible task, spawn the…
- `ralph-plan` — Planner phase of the ralph loop — pull stories from whatever tracker exists (Jira MCP, GitHub issues, or a loc…
- `tdd-loop` — Enforce RED-GREEN-REFACTOR test-driven development — failing test first, minimal code to pass, refactor on green

**Review — pre-ship checks**
- `sql-review` — Review a SQL query for plan risks, index needs, and correctness traps

**Debug — find and fix root cause**
- `log-triage` — Triage a raw log dump into clustered errors, a timeline, and the anomaly that matters
- `stacktrace-analyzer` — Analyze a stack trace or exception dump into ranked root-cause hypotheses with concrete next checks
- `systematic-debug` — Structured root-cause debugging — reproduce, observe, hypothesize, isolate, fix the cause, add the regression …

**Lookup — find code/data/facts mid-task**
- `code-search` — Find code and context efficiently — the lexical → structural → semantic search ladder with token-budget discip…
- `crawl4ai` — Complete toolkit for web crawling and data extraction using Crawl4AI
- `data-query` — Query JSON/YAML/structured data precisely with jq, gron, and yq instead of grepping it
- `dependency-lookup` — Answer "where does this symbol/class/package come from" and "who pulls this dependency in" across Maven/Java, …
- `file-find` — Find files fast by name, age, or size — fd for scoped search, plocate for instant system-wide indexed lookup, …
- `git-search` — Search git history efficiently — pickaxe, regex diffs, line-range history, rename-following, true-origin blame
- `image-ocr` — Extract text from images (JPG, PNG, WEBP) deterministically using Tesseract OCR via pytesseract, returning str…
- `paper-notes` — Summarize a paper or technical blog post into structured notes plus a relevance-to-my-stack verdict
- `read-diagram` — Parse text-based diagram formats — Mermaid, PlantUML, Lucidchart text/CSV exports, and SVG structure — into a …
- `read-image` — Extract structured content from raster images — screenshots, charts, diagram photos, whiteboards, UI mockups, …
- `repo-orient` — Orient in an unfamiliar codebase fast — entry points, build/run/test commands, layout, hot paths
- `symbol-lookup` — Find symbol definitions, references, and approximate call hierarchies — the ctags/ast-grep lane between grep a…
- `system-lookup` — Find what's happening on a machine — which process holds a port or file, what a process is running with, where…
- `web-research` — Search, fetch, and extract web content efficiently — query operators, llms.txt probing, reader/extraction ladd…

**Deliver — commits, PRs, docs, artifacts**
- `adr-lite` — Write a generic Nygard-format Architecture Decision Record
- `chart` — Render charts headlessly with matplotlib for reports, decks, and docs — Agg backend, figure lifecycle, savefig…
- `docx` — Create and edit Word documents (.docx) programmatically with python-docx — headings, styled paragraphs, tables…
- `pdf` — Read, merge, split, fill, and create PDFs with pypdf and reportlab — text extraction, page ops, form filling, …
- `pptx` — Build and edit PowerPoint decks (.pptx) with python-pptx — slides, layouts, placeholders, text formatting, ima…
- `runbook-writer` — Turn a procedure into an operational runbook with verification after every step
- `xlsx` — Create, read, and edit Excel workbooks (.xlsx) with openpyxl — formulas, number formats, styling, charts, free…

**Session — context, cost, handoffs**
- `caching` — Caching strategy across the AI stack — prompt/prefix caching, MCP and tool-response caching, session file-read…
- `reasoning-budget-guidance` — Pick the model tier and reasoning effort per task class instead of defaulting to maximum everywhere

**Util — library how-tos for scripts**
- `archive` — Create and extract zip/tar archives safely — zip-slip protection, compression settings, permission semantics
- `config` — Read and write JSON, YAML, and TOML config safely — safe_load only, the YAML Norway problem, datetime serializ…
- `data-csv` — Read, clean, and write CSV/tabular data with pandas (and stdlib csv for streaming) — dtype control, encodings,…
- `http-requests` — Call HTTP APIs robustly with requests — sessions, retries with backoff, timeouts, streaming, auth, corporate-p…
- `image` — Manipulate images with Pillow — resize, convert, crop, compose, thumbnails, EXIF orientation, format conversion
- `markup` — Parse and extract from HTML and XML — BeautifulSoup selectors, parser choice, encodings, and the XML namespace…
- `sqlite` — Use SQLite from Python (stdlib sqlite3) correctly — parameterized queries, transactions, Row access, WAL, prag…

**Meta — build the toolbox itself**
- `mcp-builder` — Conventions for building a FastMCP server that is safe and pleasant for agents to use
- `setup-instructions` — Scaffold the layered AI-instruction architecture in any repository — global CLAUDE.md, per-directory CLAUDE.md…

**Career — interview and resume practice**
- `interview-drill` — Run interactive interview practice — system design or behavioral — with follow-ups and a scored debrief
<!-- INDEX:END -->
