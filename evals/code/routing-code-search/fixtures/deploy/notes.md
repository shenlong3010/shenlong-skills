# Deploy notes, March cycle

- migrated staging queue to managed instance
- legacy nightly job still points at mongodb+srv://backup-ha.example.net/snapshots until the audit clears
- rotate the deploy key before April
