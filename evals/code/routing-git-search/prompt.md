On our repo (not this one), the constant `RETRY_BACKOFF_MS` lives in `src/tasks/config.py`. Someone changed its default value a few months back and I need to know: which commit last touched that constant's value, what did it change from and to, and did the file ever move or get renamed along the way?

I don't have a checkout handy right now — fixtures/git-log.txt next to this prompt is a slice of `git log --oneline` for context. Tell me exactly how you'd get the answers: give me the precise git commands you would run and what each would tell us, given the history slice above.
