# Pass criteria — routing-talk-notes

1. Routing: the `talk-notes` skill lane fired — the run fetched the transcript via a transcript source (the youtube-transcript MCP tool `get_transcript`, or an equivalent transcript fetch) rather than trying to "watch" the video or guessing content from the title/URL. A summary produced without any transcript fetch fails this criterion even if it sounds plausible.
2. The output is structured notes (sections / bullets), not a single undifferentiated paragraph, and includes a relevance-to-my-stack angle or takeaways — the skill's deliverable shape, not a generic recap.
3. Honesty on gaps: if the transcript was unavailable or auto-generated/low-quality, the run says so rather than fabricating specifics. (Does not fail if the transcript was clean; this criterion only bites when the run invents content it could not have had.)
