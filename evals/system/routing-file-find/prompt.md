I'm cleaning up before an audit. Two questions, both about files not contents:

1. Which file in the `fixtures/` tree next to this prompt is the biggest, and how big is it?
2. Are there any `.bak` or `~`-suffixed backup files anywhere under `fixtures/`, including hidden directories?

Just names and sizes — I don't need to see what's inside them.
