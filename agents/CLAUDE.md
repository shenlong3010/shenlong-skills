# agents/ — directory guide

Loaded when working in this directory; root globals apply. Agents are subagents — reviewers and critics — as flat `.md` files.

## Structure
**Role** (what this critic is for, and its paired maker where one exists — `plan-reviewer` ↔ `plan-writer`) → **Method** (passes or checklist, in execution order) → **Output** (the contract below).

## Output contract
- Findings severity-ranked (`blocker/major/minor/nit` for code, `fatal/major/minor` for plans — match the file's existing scale).
- Every finding cites the exact location (`file:line`, plan line, or section) plus the issue and a one-line concrete fix.
- End with a verdict the caller can act on, as a **literal greppable marker line**: `VERDICT: <approve|fix-majors|rewrite>` — every review in a transcript is then auditable with one `rg 'VERDICT:'`.

## Laws
- **Critics expose; they don't rewrite.** The maker fixes, the checker checks — no silent rewrites inside a review.
- **The correlated-blind-spot caveat is mandatory** in every reviewer: same-model review is pre-review, not a gate; a pass is weak evidence, a fail is strong.
- **Scope honesty:** end by naming what the pass cannot see (the `security-review` closing pattern).
- Every agent carries a `flow:` key (vocabulary in `tools/validate.py`) — the workflow stage it serves; drives the generated catalog below.
- New agents via `/create agent <name>`.

## Catalog (generated — do not hand-edit)

Grouped by flow. Regenerate with `python3 tools/gen-index.py` after any add/rename.

<!-- INDEX:BEGIN (generated by tools/gen-index.py) -->
**Plan — shape the work before code**
- `plan-reviewer` ·process· — Adversarial reviewer for PLAN files — the checker paired with plan-writer's maker
- `pre-mortem` ·process· — Prospective hindsight on a plan or decision — assume it already failed, explain why

**Execute — build it (loop or TDD)**
- `ralph-coder` ·agent· — Implementor role of the ralph loop — executes exactly one task file to its acceptance criteria, fresh context,…
- `ralph-diagnostician` ·agent· — Deep-dive debugger role of the ralph loop — invoked only when a task has exhausted its retry budget; diagnoses…
- `ralph-task-inspector` ·agent· — Verifier role of the ralph loop — checks a coded task against its acceptance criteria and returns one of four …

**Review — pre-ship checks**
- `code-review` ·code· — Two-stage code review of a diff or PR — spec compliance first, then code quality — with severity-ranked findin…
- `security-review` ·code· — Pre-ship security pass over a diff or module — injection, authz gaps, secrets, unsafe deserialization, SSRF, d…
<!-- INDEX:END -->
